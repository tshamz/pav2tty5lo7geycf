{
  "name": "notifications",
  "private": true,
  "version": "0.0.1",
  "engines": {
    "node": "12.x"
  },
  "scripts": {
    "dev": "nodemon .",
    "start": "node .",
    "gcp-build": "echo 'Starting Build...'",
    "deploy": "gcloud app deploy --quiet"
  },
  "dependencies": {
    "services": "*",
    "server": "*",
    "websocket": "*",
    "node-fetch": "^2.6.1",
    "reconnecting-websocket": "^4.4.0",
    "wait": "^0.4.2",
    "ws": "^7.4.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
