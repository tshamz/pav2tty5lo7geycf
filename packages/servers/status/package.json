{
  "private": true,
  "name": "status",
  "version": "0.0.1",
  "engines": {
    "node": "12"
  },
  "scripts": {
    "dev": "nodemon --watch . --watch ../../services/",
    "start": "node .",
    "deploy": "gcloud app deploy --quiet"
  },
  "yarn.build": {
    "input": ".",
    "output": "dist"
  },
  "dependencies": {
    "@services/debug": "workspace:^0.0.1",
    "@services/dotenv": "workspace:^0.0.1",
    "@services/express": "workspace:^0.0.1",
    "@services/logger": "workspace:^0.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
